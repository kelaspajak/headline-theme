<article class="gh-card post featured">
                        <a class="gh-card-link" href="{{url}}">
                            {{#if feature_image}}
                            <img class="gh-card-image" src="{{feature_image}}" alt="{{title}}" loading="lazy" />
                            {{/if}}
                            <div class="gh-card-content">
                                <span class="gh-card-meta">{{primary_tag.name}} <span class="bull">⁃</span> {{reading_time minute=(t "1 min read") minutes=(t "% min read")}}</span>
                                <h2 class="gh-card-title">{{title}}</h2>
                                <p>{{excerpt}}</p>
                                <strong>{{t "Read more"}}</strong>
                            </div>
                        </a>
                    </article>
